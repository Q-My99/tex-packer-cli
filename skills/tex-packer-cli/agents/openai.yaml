interface:
  display_name: "Tex Packer CLI"
  short_description: "Pack and split game texture atlases"
  default_prompt: "Use $tex-packer-cli to pack these sprites into a game-ready texture atlas."
policy:
  allow_implicit_invocation: true
